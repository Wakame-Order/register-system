<%= form_for(@klass) do |f| %>
  <% if @klass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@klass.errors.count, "error") %> prohibited this klass from being saved:</h2>

      <ul>
      <% @klass.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :klass_name %><br>
    <%= f.text_field :klass_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
